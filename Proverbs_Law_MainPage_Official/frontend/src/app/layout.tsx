import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "ProVerBs Legal AI Platform",
  description: "Lawful vs. Legal: Dual Analysis Adappt'plication — Professional Legal AI System | Multi-Module Platform | Powered by Advanced AI",
  keywords: ["legal ai", "proverbs", "adappt-i", "legal research", "document analysis", "case management"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
